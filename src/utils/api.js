// const values = 1
// const headers = 1
// const BASE_URL = 1
// const getResponseData = 1

// // Список сотрудников
// const getEmployeer = (id) => {
//   return fetch(`${BASE_URL}/.../`, {
//     method: "GET",
//     headers,
//     body: JSON.stringify({
//       FIO: values.FIO,
//       rate: values.rate,
//       position: values.position,
//       status: values.status,
//     }),
//   }).then(getResponseData);
// };

// // Создание ИПР?
// export const createIPR = (values) => {
//   return fetch(`${BASE_URL}/.../`, {
//     method: "POST",
//     headers,
//     body: JSON.stringify({
//       IPR: values.IPR,
//       data: values.data,
//       status: values.status,
//     }),
//   }).then(getResponseData);
// };

// // Редактирование ИПР
// export const editIPR = (values) => {
//   return fetch(`${BASE_URL}/.../`, {
//     method: "PATCH",
//     headers,
//     body: JSON.stringify({
//       IPR: values.IPR,
//       data: values.data,
//       status: values.status,
//     }),
//   }).then(getResponseData);
// };

// // Создание задачи
// export const createTask = (values) => {
//   return fetch(`${BASE_URL}/.../`, {
//     method: "POST",
//     headers,
//     body: JSON.stringify({
//       task: values.task,
//       data: values.data,
//       status: values.status,
//       description: values.description,
//       commentEmployeer: values.commentEmployeer,
//       commentBoss: values.commentBoss,
//       score: values.score,
//     }),
//   }).then(getResponseData);
// };
